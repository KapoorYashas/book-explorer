/**
 * BookCard.tsx
 * This folder (views/components) contains highly reusable UI elements.
 * Components are mostly "dumb", receiving props and emitting events.
 */
export default function BookCard() { return null; }
